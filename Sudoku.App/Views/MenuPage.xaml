<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Sudoku.App.ViewModels"
             x:Class="Sudoku.App.Views.MenuPage"
             x:DataType="vm:MenuViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{AppThemeBinding Light={StaticResource PageBackgroundLight}, Dark={StaticResource PageBackgroundDark}}">

    <Grid RowDefinitions="*,Auto" Padding="24">
        <VerticalStackLayout VerticalOptions="Center" Spacing="8">
            <Label Text="SUDOKU ZEN" FontSize="32" HorizontalTextAlignment="Center" CharacterSpacing="3"
                   TextColor="{AppThemeBinding Light=#3d3632, Dark=#e0d8cf}" />
            <Label Text="SOLVE FOR ONE" FontSize="11" CharacterSpacing="6" HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light=#8a7e74, Dark=#7a7068}" />

            <!-- Decorative separator -->
            <BoxView HeightRequest="1" WidthRequest="60" HorizontalOptions="Center"
                     Color="{AppThemeBinding Light=#c4b5a4, Dark=#4a4038}" Margin="0,20,0,12" />

            <Button Text="{Binding SavedGameInfo, StringFormat='Continue — {0}'}"
                    IsVisible="{Binding HasSavedGame}" BackgroundColor="#7d8c6e"
                    TextColor="White" FontSize="16"
                    HeightRequest="56" CornerRadius="16" Margin="0,8,0,0"
                    Command="{Binding ContinueGameCommand}" />

            <ActivityIndicator IsRunning="{Binding IsGenerating}" IsVisible="{Binding IsGenerating}"
                               Color="#7d8c6e" HeightRequest="40" Margin="0,16,0,0" />

            <Label Text="NEW GAME" FontSize="10" CharacterSpacing="3" HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light=#8a7e74, Dark=#7a7068}" Margin="0,20,0,8" />

            <Button Text="Gentle" TextColor="#8a9a7b" Style="{StaticResource DifficultyButton}"
                    Command="{Binding NewGameCommand}" CommandParameter="Easy" />
            <Button Text="Steady" TextColor="#b5a67d" Style="{StaticResource DifficultyButton}"
                    Command="{Binding NewGameCommand}" CommandParameter="Medium" />
            <Button Text="Challenging" TextColor="#c49a6c" Style="{StaticResource DifficultyButton}"
                    Command="{Binding NewGameCommand}" CommandParameter="Hard" />
            <Button Text="Deep" TextColor="#a67563" Style="{StaticResource DifficultyButton}"
                    Command="{Binding NewGameCommand}" CommandParameter="Expert" />
            <Button Text="Profound" TextColor="#8b6d7b" Style="{StaticResource DifficultyButton}"
                    Command="{Binding NewGameCommand}" CommandParameter="Evil" />
        </VerticalStackLayout>

        <Button Text="&#9881; Settings" BackgroundColor="Transparent"
                TextColor="{AppThemeBinding Light=#8a7e74, Dark=#7a7068}"
                FontSize="14" Clicked="OnSettingsClicked" Grid.Row="1"
                HorizontalOptions="Center" Margin="0,0,0,16" />
    </Grid>
</ContentPage>
