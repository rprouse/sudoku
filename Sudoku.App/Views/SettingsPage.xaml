<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Sudoku.App.ViewModels"
             x:Class="Sudoku.App.Views.SettingsPage"
             x:DataType="vm:SettingsViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{AppThemeBinding Light={StaticResource PageBackgroundLight}, Dark={StaticResource PageBackgroundDark}}">

    <Grid RowDefinitions="Auto,*" Padding="24,16">
        <HorizontalStackLayout Spacing="12">
            <Button Text="&#9664;" BackgroundColor="Transparent"
                    TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"
                    FontSize="18" Clicked="OnBackClicked" />
            <Label Text="Settings" FontSize="22" FontAttributes="Bold"
                   VerticalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#e8e8e8}" />
        </HorizontalStackLayout>

        <ScrollView Grid.Row="1" Margin="0,16,0,0">
            <VerticalStackLayout Spacing="0">
                <Label Text="APPEARANCE" FontSize="11" CharacterSpacing="2" TextColor="#2970c2" Margin="0,0,0,12" />

                <Grid ColumnDefinitions="*,Auto" Padding="0,14">
                    <VerticalStackLayout>
                        <Label Text="Dark Theme" FontSize="15"
                               TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#e8e8e8}" />
                        <Label Text="Toggle between dark and light" FontSize="12"
                               TextColor="{AppThemeBinding Light=#999999, Dark=#666666}" />
                    </VerticalStackLayout>
                    <Switch IsToggled="{Binding IsDarkTheme}" OnColor="#2970c2"
                            Grid.Column="1" VerticalOptions="Center" />
                </Grid>

                <Label Text="GAMEPLAY" FontSize="11" CharacterSpacing="2" TextColor="#2970c2" Margin="0,24,0,12" />

                <Grid ColumnDefinitions="*,Auto" Padding="0,14">
                    <VerticalStackLayout>
                        <Label Text="Highlight Related Cells" FontSize="15"
                               TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#e8e8e8}" />
                        <Label Text="Shade row, column, and box" FontSize="12"
                               TextColor="{AppThemeBinding Light=#999999, Dark=#666666}" />
                    </VerticalStackLayout>
                    <Switch IsToggled="{Binding HighlightRelatedCells}" OnColor="#2970c2"
                            Grid.Column="1" VerticalOptions="Center" />
                </Grid>

                <Grid ColumnDefinitions="*,Auto" Padding="0,14">
                    <VerticalStackLayout>
                        <Label Text="Highlight Same Numbers" FontSize="15"
                               TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#e8e8e8}" />
                        <Label Text="Tint cells with matching value" FontSize="12"
                               TextColor="{AppThemeBinding Light=#999999, Dark=#666666}" />
                    </VerticalStackLayout>
                    <Switch IsToggled="{Binding HighlightSameNumbers}" OnColor="#2970c2"
                            Grid.Column="1" VerticalOptions="Center" />
                </Grid>

                <Grid ColumnDefinitions="*,Auto" Padding="0,14">
                    <VerticalStackLayout>
                        <Label Text="Show Errors" FontSize="15"
                               TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#e8e8e8}" />
                        <Label Text="Highlight incorrect values immediately" FontSize="12"
                               TextColor="{AppThemeBinding Light=#999999, Dark=#666666}" />
                    </VerticalStackLayout>
                    <Switch IsToggled="{Binding ShowErrors}" OnColor="#2970c2"
                            Grid.Column="1" VerticalOptions="Center" />
                </Grid>

                <Grid ColumnDefinitions="*,Auto" Padding="0,14">
                    <VerticalStackLayout>
                        <Label Text="Auto-Remove Candidates" FontSize="15"
                               TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#e8e8e8}" />
                        <Label Text="Remove candidates when placing a number" FontSize="12"
                               TextColor="{AppThemeBinding Light=#999999, Dark=#666666}" />
                    </VerticalStackLayout>
                    <Switch IsToggled="{Binding AutoRemoveCandidates}" OnColor="#2970c2"
                            Grid.Column="1" VerticalOptions="Center" />
                </Grid>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
